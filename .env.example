# Obsidian MCP Server Configuration

# =============================================================================
# VAULT CONFIGURATION (Required)
# =============================================================================
# Path to your Obsidian vaults directory on the HOST machine
# This directory should contain subdirectories, each being an Obsidian vault
# Example structure:
#   /home/user/obsidian-vaults/
#   ├── personal/       → becomes vault "personal"
#   ├── work/           → becomes vault "work"
#   └── notes/          → becomes vault "notes"
#
# The server auto-discovers all subdirectories as individual vaults
VAULT_PATH=/path/to/your/vaults

# =============================================================================
# MCP SERVER CONFIGURATION
# =============================================================================
# Port for MCP server to listen on
#MCP_PORT=8080

# Enable/disable authentication
#MCP_AUTH_ENABLED=false

# API token for authentication (required if MCP_AUTH_ENABLED=true)
# Generate a secure token: openssl rand -hex 32
#MCP_AUTH_TOKEN=your-secret-token-here

# =============================================================================
# COMMAND EXECUTION SETTINGS
# =============================================================================
# Command execution timeout in seconds
#COMMAND_TIMEOUT=30

# Maximum output size in characters (output truncated from beginning if exceeded)
#MAX_OUTPUT_SIZE=100000

# =============================================================================
# COMMAND WHITELIST SETTINGS
# =============================================================================
# Enable/disable command whitelist
# When enabled, only commands in whitelist.txt can be executed
#WHITELIST_ENABLED=true
